# sunrise-surge-rules

Public runtime assets for the private `sunrise-surge` configuration.

This repository intentionally contains only public rule files and the Sub-Store node rename script. It does not contain private Surge profiles or subscription URLs.

The private runtime profile lives in `sunrise0o0/sunrise-surge`.

## Rule URL pattern

```text
https://raw.githubusercontent.com/sunrise0o0/sunrise-surge-rules/Master/Rules/AIGC.list
```

Cursor has a dedicated rule file:

```text
https://raw.githubusercontent.com/sunrise0o0/sunrise-surge-rules/Master/Rules/Cursor.list
```

## Sub-Store node rename script

```text
https://raw.githubusercontent.com/sunrise0o0/sunrise-surge-rules/Master/Module/Spec/Sub-Store/Moore/Node-Rename.js#provider=TAG&flag&one&keep=GPT+NF+IPLC&blockquic=off
```
